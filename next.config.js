// File: /Users/joshkennedy/Documents/Pord/next.config.js
/** @type {import('next').NextConfig} */
module.exports = {
    images: {
      // either style works; this is the simplest
      domains: ["upload.wikimedia.org", "images.unsplash.com"],
      // (remotePatterns would also work)
    },
  };
  